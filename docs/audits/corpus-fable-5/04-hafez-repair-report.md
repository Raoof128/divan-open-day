# Corpus repair — Phase 3 Hafez report

Final state: **60 unique canonical Hafez ghazals** (24 Bell + 36 Clarke), every record
regenerated by `scripts/poetry/build-production-corpus.ts` under
`scripts/poetry/fable5-repair-spec.ts`, all with fresh
`source-bound-alignment-v3-fable5-repair` authorities (2026-07-17).

## Bell (24 records) — structural + recovery repairs

1. **Full opening couplet published** (all 24). Previously a single hemistich carried both
   English lines through a false line-level mapping. Now: both hemistichs of the matla
   (verbatim from the repaired extraction), `alignment: stanza`, both English lines mapped
   to the couplet whole, with the disclosure that Bell renders the couplet freely as a
   stanza. This replaces a wrong claim with a weaker, true one.
2. **Ghazal 65 hemistich recovered** (D-02): `خوشتر ز عیش و صحبت و باغ و بهار چیست` — the
   extractor's footnote-reference bug is fixed and regression-locked
   (`tests/content/hafezGhazalExtraction.test.ts`); the re-extraction recovered truncations
   in 162/494 ghazals, of which ghazal 65 is the only one inside a published window.
3. **163 wrap join** (D-03): "Nor merry the Spring without the sweet laughter of wine ;" —
   the printed line wrap rejoined per the archive text, disclosed.
4. **Numerals recovered** (D-08): 090 → `Bell poem III` (was OCR `Ul`); 336 →
   `Bell poem XLIII` (was a wrong `XLII` that duplicated 169's citation). A generator guard
   now rejects any non-Roman numeral, and the corpus-integrity suite forbids duplicate Bell
   citations.
5. **Drop-caps normalised + disclosed** (D-09): 046 "The rose has flushed red…", 288
   "The margin of a stream…" — same class and same disclosure as the pre-existing 12.
6. **The 12 hand-typed opening corrections** (D-13): each verified verbatim in the locked
   archive text during Phase 2; the map now carries that provenance in code comments and
   every application is disclosed per record.

## Clarke (36 records) — provenance + OCR repairs

1. **Provenance rebound** (D-11): `english_source_sha256` now names the locked transcript
   actually read (`volume-1.txt` `ff0642a5…` / `volume-2.txt` `ac0ea92c…`); the PDF scans
   stay locked as acquisition/visual evidence, and the rationale states the chain. The
   corpus-integrity suite enforces text-artifact binding for every Clarke record.
2. **Five scan-proven OCR corrections** (D-04): 350 `Iii→In`, 489 `O them→O thou`,
   034 `(0→(O` + garbled radif dash, 130 `(0→(O`. Each correction's premise (the artefact
   exists in the transcript) and reading (independent tesseract page + rendered scan) is
   verified; the generator refuses a correction whose premise fails.
3. **Three jammed couplets restructured**: 091 (found in the ledger) plus **038 and 089
   (found by the generator's new line-count guard during this campaign)** — each had both
   verse lines jammed into one truncated line (`…yet with heart, friend`, `…save the`,
   `…from th`). Both complete lines recovered from the transcript, mapped 1:1, disclosed.
4. Confidence values for v3 records are rounded to two decimals; the alignment verdicts are
   carried from v2 (rationale says so explicitly — this campaign did not re-verify the 31
   textually-untouched Clarke alignments beyond binding and structural checks).

## Verification state

Corpus-integrity suite 13/13 · full unit suite green · e2e 5/5 · two-build byte
reproducibility proven · unique canonical identities enforced at compile time and re-checked
in Phase 10.
